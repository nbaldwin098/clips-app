VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
# VITE_STRIPE_PAYMENT_LINK is NOT used — own Checkout Session via Edge Function
# STRIPE_SECRET_KEY belongs in Supabase Edge Function secrets (never VITE_*)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
VITE_ADMIN_CODE=change-this-admin-code
VITE_PLATFORM_OWNER_ID=your-supabase-user-uuid
# Live / OBS (optional). OBS Studio is free — window share works without these.
# Set RTMP only when a real ingest server exists (never invent a URL):
# VITE_LIVE_RTMP_URL=rtmp://ingest.example.com/live
# VITE_LIVE_INGEST_CONNECTED=true
# Deploy: supabase functions deploy create-checkout-session (see docs/OWN_CHECKOUT.md)
# Sign-in: Authentication → Providers → Email (Apple needs paid Apple Developer).
# Email Templates + SMS must say Clips. Copy the text from Help on the site.
# Server only (Edge Function secret, not Render VITE_*):
# STRIPE_SECRET_KEY=sk_test_...
