import SpaShell from '../SpaShell'
import { sectionMetadata } from '../sectionMeta'

export const metadata = sectionMetadata('studio-tools')

export default function StudioToolsRoute() {
  return <SpaShell />
}
