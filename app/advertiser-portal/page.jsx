import SpaShell from '../SpaShell'
import { sectionMetadata } from '../sectionMeta'

export const metadata = sectionMetadata('advertiser-portal')

export default function AdvertiserPortalRoute() {
  return <SpaShell />
}
