import SpaShell from '../SpaShell'
import { sectionMetadata } from '../sectionMeta'

export const metadata = sectionMetadata('subscriptions')

export default function SubscriptionsRoute() {
  return <SpaShell />
}
