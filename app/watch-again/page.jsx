import SpaShell from '../SpaShell'
import { sectionMetadata } from '../sectionMeta'

export const metadata = sectionMetadata('watch-again')

export default function WatchAgainRoute() {
  return <SpaShell />
}
