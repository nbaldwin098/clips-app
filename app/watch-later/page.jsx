import SpaShell from '../SpaShell'
import { sectionMetadata } from '../sectionMeta'

export const metadata = sectionMetadata('watch-later')

export default function WatchLaterRoute() {
  return <SpaShell />
}
