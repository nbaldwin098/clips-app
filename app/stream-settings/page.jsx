import SpaShell from '../SpaShell'
import { sectionMetadata } from '../sectionMeta'

export const metadata = sectionMetadata('stream-settings')

export default function StreamSettingsRoute() {
  return <SpaShell />
}
