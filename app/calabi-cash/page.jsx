import SpaShell from '../SpaShell'
import { sectionMetadata } from '../sectionMeta'

export const metadata = sectionMetadata('calabi-cash')

export default function CalabiCashRoute() {
  return <SpaShell />
}
