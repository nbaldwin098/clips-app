// Removed: Cost Simulator was an internal planning tool, not product UI.
// Primary product path is Import Short (link-only, zero binary storage).
export default function CostSimulatorModal() { return null }
